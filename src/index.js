import React from 'react';
import ReactDOM from 'react-dom';
import SinglePieceGrid from './components/SinglePieceGrid';

ReactDOM.render(
  <React.StrictMode>
    <SinglePieceGrid />
  </React.StrictMode>,
  document.getElementById('root')
);

